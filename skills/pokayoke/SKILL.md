---
name: pokayoke
description: Require every important rule to have both a declaration and an enforcement. Use when a policy exists only on paper or a lock exists without a written rule.
---

# POKAYOKE

A sign is not a lock. The worst state is sign still up, lock gone.

```bash
python3 skills/pokayoke/scripts/pokayoke.py
python3 skills/pokayoke/scripts/pokayoke.py --selftest
```
